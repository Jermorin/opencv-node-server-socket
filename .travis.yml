language: node_js
os:
    - osx
before_install:
    - brew update
    - brew upgrade
    - brew install cmake
    - brew tap homebrew/science
    - brew install opencv
    - brew install node
after_script:
  - 'npm run coveralls'
